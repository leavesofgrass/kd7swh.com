{
  "name": "kd7swh-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "eleventy --serve --incremental --port=8080",
    "build": "eleventy",
    "publish": "node tools/publish.mjs",
    "guard": "node tools/guard.mjs"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "@11ty/eleventy-plugin-rss": "^3.0.0"
  }
}
